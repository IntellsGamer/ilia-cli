# {{ project_name }}

{{ description }}

## Setup

1. Create a bot at https://discord.com/developers/applications
2. Copy your token to `.env` or `bot.py`
3. Run: `pip install -r requirements.txt && python bot.py`
