{
  "name": "{{ project_name }}",
  "version": "{{ version }}",
  "private": true,
  "scripts": {
    "dev": "bun --watch run src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "elysia": "^1.2.0"
  },
  "devDependencies": {
    "bun-types": "^1.2.0"
  }
}
